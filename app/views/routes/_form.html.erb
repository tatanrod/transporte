<%= form_with(model: route, local: true) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude_origin %>
    <%= form.text_field :latitude_origin, id: :route_latitude_origin %>
  </div>

  <div class="field">
    <%= form.label :latitude_destination %>
    <%= form.text_field :latitude_destination, id: :route_latitude_destination %>
  </div>

  <div class="field">
    <%= form.label :length_origin %>
    <%= form.text_field :length_origin, id: :route_length_origin %>
  </div>

  <div class="field">
    <%= form.label :length_destination %>
    <%= form.text_field :length_destination, id: :route_length_destination %>
  </div>

  <div class="field">
    <%= form.label :date_rou %>
    <%= form.date_select :date_rou, id: :route_date_rou %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
