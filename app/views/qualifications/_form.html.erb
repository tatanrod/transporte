<%= form_with(model: qualification, local: true) do |form| %>
  <% if qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qualification.errors.count, "error") %> prohibited this qualification from being saved:</h2>

      <ul>
      <% qualification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value_cal %>
    <%= form.number_field :value_cal, id: :qualification_value_cal %>
  </div>

  <div class="field">
    <%= form.label :date_cal %>
    <%= form.date_select :date_cal, id: :qualification_date_cal %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :qualification_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
